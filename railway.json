{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "gunicorn ndnstore.wsgi:application --bind 0.0.0.0:$PORT"
  },
  "source": "backend"
}