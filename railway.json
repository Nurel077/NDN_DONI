{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn ndnstore.wsgi:application --bind 0.0.0.0:$PORT"
  }
}